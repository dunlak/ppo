#ifndef KEYBOARDTSLCD_H_
#define KEYBOARDTSLCD_H_

#include "KeyboardTs.h"
#include "LedLcd.h"

class KeyboardTsLcd {
	public:
		KeyboardTsLcd(unsigned char = 0);
		enum KeyboardState eRead(void);
	private:
		LedLcd* pLed;
		KeyboardTs* pKeyboard;
};

#endif /* KEYBOARDTSLCD_H_ */
